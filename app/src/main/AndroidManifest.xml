<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffeeshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coffeeshop">
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.CoffeeDetailActivity" />
        <activity android:name=".ui.FindUserFragment" />
        <activity android:name=".ui.Order" />
        <activity android:name=".ui.DrinksActivity" />
        <activity android:name=".ui.CappuccinoActivity" />
        <activity android:name=".ui.FilterActivity" />
        <activity android:name=".ui.CoffeeActivity" />
        <activity android:name=".ui.CoffeeChoose" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
